# This is a blueprint of the environment variables
# Duplicate this file as .env in the root of the project
# and update the environment variables with the matching values

# The number of the HTTP port for the app to run
PORT=

# The database values
HOST=
DATABASE=
DB_USER=
DB_USER_PASSWORD=